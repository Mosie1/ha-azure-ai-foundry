# Normalize line endings to LF in the repository for all text files.
# This keeps CI (Linux) and editors consistent regardless of core.autocrlf.
* text=auto eol=lf

# Explicitly treat common binary types as binary.
*.png binary
*.jpg binary
*.ico binary
